module.exports = class PageInfo {

    constructor() {
        /** File name of page. */
        this.name = "";
        /** Size of small thumbnail on y-axis. */
        this.height = 0;
        /** Size of small thumbnail on x-axis. */
        this.width = 0;
    }
}